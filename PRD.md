# 📝 상품 요구사항 정의서 (PRD.md)

이 문서는 업비트(메인 전략) 및 빗썸(서브 전략) 암호화폐 퀀트 자동 매매 시스템의 상품 요구사항을 정의합니다.

---

## 1. 시스템 개요
- **목표**: 메인(업비트) 및 서브(빗썸) 전략별 거래소 API를 연동한 리밸런싱 방향 지시(시그널) 및 잔고 보고서 자동 발송
- **기본 환경**: GitHub Actions 자동 구동 환경 (조회 전용 시그널 알림 봇) 및 로컬 시뮬레이션 환경

---

## 2. 핵심 기능 정의

### ① 메인 전략 (업비트 - BTC & ETH)
- **목표 비중**: 계좌 잔고 내 BTC 50% / ETH 50%
- **지표 필터**:
  - **BTC**: 200일 SMA 대비 ±2% 노이즈 필터링
  - **ETH**: 150일 SMA 대비 14일 ATR의 1.5배 필터링
- **리밸런싱**: 두 자산 모두 보유 시 한쪽 비중이 목표 비중(50%) 대비 ±10%p 초과 이탈 시 50:50으로 재조정. (한쪽만 보유 시에는 비중 리밸런싱 미실행)

### ② 서브 전략 (빗썸 - 알트코인)
- **공통 시장 필터**: 업비트 BTC 가격의 200일 SMA ±2% 기준 판정 (상승장/하락장/버퍼 구간 대기)
- **매일 점검**: 하락장 판정 시 빗썸 내 모든 알트코인 시장가 전량 청산
- **주간 리밸런싱**: 월요일 상승장 시 7일 평균 거래대금 상위 10종목 중 14일 수익률 상위 4종목을 각 25%씩 동일 비중 매수

### ③ 서브 전략(알트코인) 실행 여부 선택 기능 (단일 설정 파일 제어)
- **목표**: 빗썸 알트코인 전략을 실행할지 여부와 모의 투자 실행 여부를 **[`src/config.py`](file:///c:/Users/hakso/_work/quant-bitcoin/src/config.py) 한곳에서 모두 설정**할 수 있도록 지원합니다.
- **상수 정의**:
  - `DRY_RUN`: `True` (모의투자) 또는 `False` (실거래)
  - `USE_ALTCOIN_STRATEGY`: `True` (빗썸 기동) 또는 `False` (빗썸 비활성화)
- **구동 로직**:
  - `USE_ALTCOIN_STRATEGY`가 `False`일 경우 빗썸 연동을 전부 스킵하며, 디스코드 보고서에는 서브 전략 영역이 "비활성화(Disabled)"로 표시됩니다.
  - 배치 파일(`run_bot.bat` 및 `run_bot.example.bat`)에는 오직 보안이 필요한 **API 키 정보**만 두고 전략 옵션은 전부 배제하여 설정 창을 일원화합니다.

---

## 3. 예외 및 에러 처리
- API 실패 시 최대 3회 재시도 후 디스코드 경보 및 안전 종료
- 시장가 주문 기본 수행 및 거래소 최소 주문 금액(업비트 5,000원 / 빗썸 1,000원) 미만 스킵 안전장치
- Windows 환경 인코딩 오류(CP949) 방지를 위해 배치 파일 주석은 영문(ASCII)만 사용하고, 파이썬 파일 주석은 UTF-8 한국어로 작성
- 로컬 환경에서 가동 시 `logs/` 디렉토리에 월별로 구분된 로그 파일(`trading_log_YYYY-MM.log`)을 파이썬 내부에서 자동 분할 및 생성하여 누적 기록하며, 깃허브 액션 환경 구동 시에는 로컬 파일 생성 없이 표준 콘솔 출력으로만 실시간 로그 송출

---

## 4. 미래 대안 시나리오: 조회 전용 시그널 알림 봇 (GitHub Actions 연동)
- **개념**: 실제 매매 주문은 모바일 앱으로 직접 집행하고, 일일 분석 연산 및 리밸런싱 방향 지시(시그널)만 디코드로 자동 수신하는 시나리오입니다.
- **주요 변경 사항**:
  - **API 권한**: 업비트와 빗썸에서 [조회] 권한만 활성화하여 API 키 재발급 (IP 등록이 불필요함).
  - **시스템 설정**: `src/config.py` 내 `DRY_RUN = True` 고정 설정.
  - **실행 환경**: GitHub Actions를 다시 활성화하여 매일 오전 09:05 KST에 서버 비용 없이 무료로 자동 실행되도록 워크플로우 구성 (`.github/workflows/main.yml` 복구).
- **장점**:
  1. **비용 0원**: 개인 PC를 항상 켜둘 필요가 없고, 가상 서버(VPS) 대여료 없이 100% 무료 가동 가능.
  2. **완벽한 보안**: API 키에 주문 및 출금 권한이 없어 해킹이나 키 노출 시에도 자산 탈취 위험 없음.
  3. **IP 제약 없음**: 조회 API는 동적 IP에서도 호출 가능하므로 거래소 고정 IP 등록 불필요.
- **전환 방법**:
  1. 업비트 및 빗썸에서 '조회' 권한만 체크한 API 키를 새로 발급받아 GitHub Repository Secrets에 등록.
  2. 깃허브 액션 설정 파일(`.github/workflows/main.yml`)을 복구하여 커밋.
  3. `src/config.py` 파일의 `DRY_RUN` 변수값을 `True`로 수정.

---

## 5. 백테스트 시스템 요구사항 (KRW 업비트 시세 기준)
- **개념**: 업비트의 실제 과거 원화(KRW) 일봉 가격 데이터를 조회하여, 메인 전략(BTC & ETH 50:50 분할 및 리밸런싱, 개별 이동평균선/ATR 추세 필터링)의 역사적 성과를 검증합니다.
- **주요 기능**:
  - **데이터 수집 및 페이징**: 업비트 API를 활용해 과거 최소 3년 ~ 최장 8년 치의 KRW-BTC, KRW-ETH 일봉 데이터(시가, 고가, 저가, 종가, 거래량)를 페이징 역추적으로 다운로드하여 로컬에 캐싱 및 활용.
  - **전략 시뮬레이션 구현**:
    - **BTC**: 200일 SMA(단순이동평균) 필터링 (현재가 >= 200 SMA * 1.02 진입, 현재가 < 200 SMA * 0.98 청산/대기)
    - **ETH**: 150일 SMA 및 14일 ATR 변동성 필터링 (현재가 >= 150 SMA + 1.5 * ATR 진입, 현재가 < 150 SMA - 1.5 * ATR 청산/대기)
    - **리밸런싱**: 매일 아침 종가 확정 후 비중 편차가 ±10%p를 초과할 경우 50:50으로 재조정.
    - **수수료**: 업비트 표준 거래 수수료율 0.05%를 매수/매도 거래가 발생할 때마다 정확히 반영.
  - **성과 분석 지표 산출**:
    - 누적 수익률 (Cumulative Return) 및 단순 보유(Buy & Hold) 대비 초과 수익률 비교
    - 연평균 성장률 (CAGR)
    - 최대 낙폭 (MDD, Max Drawdown) 및 하락 구간 시각화
    - 거래 횟수, 리밸런싱 발생 횟수 및 연도별 수익률 요약
  - **다양한 백테스트 시나리오 실행 옵션**:
    - 사용자가 매번 소스 코드를 수정하지 않고 분석 조건을 변경해 가며 실행할 수 있도록 아래의 옵션을 지원합니다.
      - `이동평균 기간 조정`: BTC 및 ETH의 SMA 연산 기간 개별 설정 (`--btc-sma`, `--eth-sma`)
      - `분석 기간 및 원금 조정`: 과거 백테스트 일수 및 시작 금액 설정 (`--days`, `--capital`)
      - `리밸런싱 활성/비활성`: 포트폴리오 차원의 50:50 비중 재조정 거래를 배제하고 두 자산을 독립적으로 운용하는 모드 (`--no-rebalance`)
      - `파라미터 최적화`: 이평선 기간 조합별 성과를 연산하여 최고 수익률 순으로 정렬하는 모드 (`--optimize`)
  - **구현 방식**: `src/backtest.py` 스크립트를 신설하여 백테스트를 실행하고, 성과 보고서와 가격 추이 그래프 이미지(Matplotlib 활용)를 저장할 수 있도록 구현.

