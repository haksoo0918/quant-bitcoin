# 📝 상품 요구사항 정의서 (PRD.md)

이 문서는 업비트(메인 전략) 및 빗썸(서브 전략) 암호화폐 퀀트 자동 매매 시스템의 상품 요구사항을 정의합니다.

---

## 1. 시스템 개요
- **목표**: 퀀트 전략(업비트 메인 & 빗썸 서브)에 따라 신호를 분석하고, 실행 환경에 따라 시그널 알림 또는 실거래 자동 주문을 완전히 분리하여 수행합니다.
- **실행 환경 이원화**:
  - **GitHub Actions (클라우드)**: **시그널 전용 모드 (`SIGNAL_ONLY`)** — 매일 09:05 KST 자동 실행. API 키 및 거래소 잔고 조회 없이 오직 **공개 시세 데이터** 만을 바탕으로 전략 방향성(매수/매도/홀딩 및 시장 상태)을 판단하여 디스코드로 브리핑합니다.
  - **Local PC (로컬 환경)**: **실제 자동매매 실행 모드 (`LIVE_TRADING`)** — `run_bot.bat` 실행 시 발급된 API 키로 실계좌 잔고를 조회하고, 전략에 따라 **거래소에 실제 매수/매도 주문을 직접 집행** 한 뒤 체결 내역과 실제 잔고 현황을 디스코드로 보고합니다.

---

## 2. 핵심 기능 정의

### ① 메인 전략 (업비트 - BTC & ETH)
- **목표 비중**: 계좌 잔고 내 BTC 50% / ETH 50%
- **지표 필터**:
  - **BTC**: 220일 SMA 대비 ±2% 노이즈 필터링
  - **ETH**: 50일 SMA 대비 14일 ATR의 1.5배 필터링
- **리밸런싱**: 두 자산 모두 보유 시 한쪽 비중이 목표 비중(50%) 대비 ±10%p 초과 이탈 시 50:50으로 재조정. (한쪽만 보유 시에는 비중 리밸런싱 미실행)

### ② 서브 전략 (빗썸 - 이더리움 SuperTrend + 50일 SMA 추세 추종)
- **개념**: 이더리움(ETH)의 높은 변동성과 강력한 추세 상승력을 극대화하면서, 약세장 진입 시 100% 현금화하여 최대 낙폭(MDD)을 극적으로 방어하는 **이더리움 단독 고수익/저낙폭 퀀트 전략**.
- **복합 지표 필터**:
  - **1. 대추세 필터 (50일 SMA)**: 일봉 종가가 50일 단순이동평균(SMA) 이상일 때만 매수 포지션을 허용하고, 50일 SMA 하향 이탈 시 즉시 전량 매도하여 하락장 리스크 원천 차단.
  - **2. 동적 변동성 추세 지표 (SuperTrend 7, 3.5)**:
    - 기간(Period) = 7, 승수(Multiplier) = 3.5 기반 SuperTrend 지표 적용.
    - SuperTrend가 상승 밴드(Green)를 유지하고 50일 SMA 위에 있을 때 **빗썸 자산 100% 이더리움 집중 매수/보유**.
    - SuperTrend가 하락 밴드(Red)로 전환되거나 50일 SMA를 하향 돌파하면 **즉시 100% 원화(KRW) 현금화**.
- **매매 실행 주기**: 매일 아침 09:05 KST 일봉 종가 확정 후 1회 점검 및 주문 집행.
- **백테스트 검증 성과 (최근 3.6년 실데이터)**: 누적 수익률 **+335.97%**, CAGR **+51.29%**, MDD **-25.03%**, Calmar 비율 **2.05** (승률 53.3%).

### ③ 실행 환경별 동작 및 디스코드 메시지 포맷 완전 분리
- **1. GitHub Actions (시그널 브리핑 모드)**:
  - **동작 방식**: `GITHUB_ACTIONS=true` 자동 감지 또는 `--signal-only` 인자 시 동작. API 키 및 잔고 조회를 일절 수행하지 않으며, 오직 공개 시세 API로 시장 지표와 전략 상태만 계산.
  - **디스코드 타이틀**: `📢 [GitHub Actions] 퀀트 전략 일일 방향성 시그널 브리핑`
  - **메시지 내용**:
    - **업비트 메인 전략 (BTC & ETH)**: BTC(220일 SMA ±2%), ETH(50일 SMA ± 1.5 ATR) ➔ **[매수 유지 / 신규 진입 / 현금화 관망]**
    - **빗썸 서브 전략 (이더리움 SuperTrend)**: ETH 현재가 vs 50일 SMA 및 SuperTrend(7, 3.5) 상태 ➔ **[ETH 100% 풀매수 / 100% 현금화 관망]** 방향성 브리핑
    - (가상 잔고나 가상 주문 금액은 일체 출력하지 않고 순수 전략 지표 및 방향성 신호만 명확히 전달)
- **2. Local PC (로컬 실행 모드 - CLI 옵션 제어)**:
  - **설정 방식**: API Key 및 웹훅 정보는 `.env` 파일에 보관하고, 실행 옵션은 CLI 인자(`--mode` 또는 `--dry-run`, `--live`, `--signal-only`, `--no-alt`)로 직관적으로 제어합니다.
  - **모드 구분**:
    - `시그널 브리핑`: `python src/main.py --signal-only` (API Key 불필요, 방향성 브리핑 출력)
    - `모의 매매(Dry-Run)`: `python src/main.py --dry-run` (가상 잔고 기반 모의 주문 및 디스코드 리포트)
    - `실거래(Live Trading)`: `python src/main.py --live` (실계좌 잔고 기반 실제 매수/매도 주문 집행)
  - **디스코드 타이틀**:
    - 모의 매매: `⚡ [로컬 모의매매] 모의 주문 체결 및 포트폴리오 잔고 보고서`
    - 실거래: `⚡ [로컬 자동매매] 실거래 주문 체결 및 포트폴리오 잔고 보고서`
  - **수익률 표기 사양**:
    - **통합 총 자산 및 종합 수익률**: 상단에 양대 거래소(업비트+빗썸) 총 자산 합계 및 통합 총 투자 원금 대비 전체 종합 평가수익률 표시.
    - **전략별 총 자산 수익률**: 업비트 메인 전략 및 빗썸 서브 전략별 총 자산 라인에 각각의 총 평가수익률(현금 포함 매수원금 대비 평가익) 표기.
    - **개별 종목별 수익률**: 보유 중인 코인(BTC, ETH)의 평균 매수가 대비 개별 수익률 병행 표시.

---

## 3. 예외 및 에러 처리
- API 실패 시 최대 3회 재시도 후 디스코드 경보 및 안전 종료
- 시장가 주문 기본 수행 및 거래소 최소 주문 금액(업비트 5,000원 / 빗썸 1,000원) 미만 스킵 안전장치
- `.env` 파일을 통한 민감 정보 관리 및 `.gitignore`를 통한 커밋 방지 보안 준수
- 로컬 환경에서 가동 시 `logs/` 디렉토리에 월별로 구분된 로그 파일(`trading_log_YYYY-MM.log`)을 파이썬 내부에서 자동 분할 및 생성하여 누적 기록하며, 깃허브 액션 환경 구동 시에는 로컬 파일 생성 없이 표준 콘솔 출력으로만 실시간 로그 송출

---

## 4. 실행 환경 및 운영 구조
- **GitHub Actions (수동 실행 `workflow_dispatch` 지원)**:
  - Repository Secrets: `DISCORD_WEBHOOK_URL` (1개만 등록, 거래소 API Key 불필요)
  - 실행 명령: `python src/main.py --signal-only`
  - 역할: 자동 스케줄(Cron) 지연 이슈 방지를 위해 수동 트리거 지원으로 전환. (시세 및 전략 확인은 PWA 대시보드의 실시간 시세 조회 기능 및 로컬 스케줄러 자동 실행 활용)
- **로컬 환경 (CLI 및 `.env` 기반 실행)**:
  - `.env` 파일에 API Key 및 `DISCORD_WEBHOOK_URL` 설정
  - CLI 명령어로 실행:
    - 시그널 확인: `python src/main.py --signal-only`
    - 모의 매매: `python src/main.py --dry-run`
    - 실거래 주문: `python src/main.py --live`
  - 배치 파일(`run_bot.bat`): CLI 인자 전달 및 실행 자동화 래퍼
    - **스케줄러/CLI 인자 전달 시 (`run_bot.bat --live` 등)**: 프로세스 완료 즉시 `pause` 없이 터미널 창 자동 닫힘 (무인 백그라운드 자동화).
    - **사용자 수동 더블 클릭 시 (인자 없음)**: 실행 결과 및 로그 확인을 위해 대기(`pause`) 유지.
  - Windows 작업 스케줄러 등록 스크립트(`scripts/setup_scheduler.bat`, `scripts/remove_scheduler.bat`): 매일 09:05 KST `run_bot.bat --live` 자동 등록 지원.

---

## 5. 백테스트 시스템 요구사항 (KRW 업비트 시세 기준)
- **개념**: 업비트의 실제 과거 원화(KRW) 일봉 가격 데이터를 조회하여, 메인 전략(BTC & ETH 50:50 분할 및 리밸런싱, 개별 이동평균선/ATR 추세 필터링)의 역사적 성과를 검증합니다.
- **주요 기능**:
  - **데이터 수집 및 페이징**: 업비트 API를 활용해 과거 최소 3년 ~ 최장 8년 치의 KRW-BTC, KRW-ETH 일봉 데이터(시가, 고가, 저가, 종가, 거래량)를 페이징 역추적으로 다운로드하여 로컬에 캐싱 및 활용.
  - **전략 시뮬레이션 구현**:
    - **BTC**: 200일 SMA(단순이동평균) 필터링 (현재가 >= 200 SMA * 1.02 진입, 현재가 < 200 SMA * 0.98 청산/대기)
    - **ETH**: 150일 SMA 및 14일 ATR 변동성 필터링 (현재가 >= 150 SMA + 1.5 * ATR 진입, 현재가 < 150 SMA - 1.5 * ATR 청산/대기)
    - **리밸런싱**: 매일 아침 종가 확정 후 비중 편차가 ±10%p를 초과할 경우 50:50으로 재조정.
    - **수수료**: 업비트 표준 거래 수수료율 0.05%를 매수/매도 거래가 발생할 때마다 정확히 반영.
  - **성과 분석 지표 산출**:
    - 누적 수익률 (Cumulative Return) 및 단순 보유(Buy & Hold) 대비 초과 수익률 비교
    - 연평균 성장률 (CAGR)
    - 최대 낙폭 (MDD, Max Drawdown) 및 하락 구간 시각화
    - 거래 횟수, 리밸런싱 발생 횟수 및 연도별 수익률 요약
  - **다양한 백테스트 시나리오 실행 옵션**:
    - 사용자가 매번 소스 코드를 수정하지 않고 분석 조건을 변경해 가며 실행할 수 있도록 아래의 옵션을 지원합니다.
      - `이동평균 기간 조정`: BTC 및 ETH의 SMA 연산 기간 개별 설정 (`--btc-sma`, `--eth-sma`)
      - `분석 기간 및 원금 조정`: 과거 백테스트 일수 및 시작 금액 설정 (`--days`, `--capital`)
      - `리밸런싱 활성/비활성`: 포트폴리오 차원의 50:50 비중 재조정 거래를 배제하고 두 자산을 독립적으로 운용하는 모드 (`--no-rebalance`)
      - `슬리피지(Slippage) 모델`: 시장가 체결 오차를 보수적으로 반영하기 위한 슬리피지 비율 파라미터 (`--slippage`, 기본값 0.0%)
      - `파라미터 최적화`: 이평선 기간 조합별 성과를 연산하여 최고 수익률 순으로 정렬하는 모드 (`--optimize`)
  - **결과물 저장 및 디렉토리 구조 표준화**:
    - 모든 백테스트 결과물은 전략 유형별로 `backtest/` 하위 디렉토리에 분류되어 자동 저장됩니다.
    - 실행 이력 보존과 구분을 위해 생성되는 보고서 및 차트 이미지 파일명에 실행 일시 타임스탬프(`YYYYMMDD_HHMMSS`)를 자동으로 포함합니다.
      - **메인 전략 (업비트 50:50)**:
        - 보고서: `backtest/main/backtest_report_YYYYMMDD_HHMMSS.md`
        - 자산 곡선 차트: `backtest/main/backtest_result_YYYYMMDD_HHMMSS.png`
        - 파라미터 최적화 보고서: `backtest/main/backtest_optimization_report_YYYYMMDD_HHMMSS.md`
      - **서브 전략 (빗썸 이더리움 SuperTrend + 50일 SMA)**:
        - 보고서: `backtest/sub_eth/backtest_bithumb_eth_report_YYYYMMDD_HHMMSS.md`
        - 자산 곡선 차트: `backtest/sub_eth/backtest_bithumb_eth_result_YYYYMMDD_HHMMSS.png`
  - **구현 방식 및 공통 모듈화**:
    - `src/indicators.py` 공통 기술적 지표 모듈(SMA, ATR, SuperTrend, 버퍼, 히스테리시스 연산)을 `main.py`와 `backtest.py`, `backtest_bithumb_eth.py`가 공유하여 전략 로직의 일관성을 보장합니다.
    - `src/backtest.py` 스크립트를 통해 메인 전략 백테스트를 실행하고, `src/backtest_bithumb_eth.py`를 통해 빗썸 이더리움 서브 전략 백테스트를 수행합니다.

### 5.2. 서브 전략 (빗썸 알트코인 모멘텀 2.0) 백테스트 및 개선 요구사항
- **개념**: 빗썸 KRW 마켓의 과거 일봉 데이터를 기반으로, 비트코인 공통 시장 필터(220 SMA ±2%)와 주간 거래대금/모멘텀 상위 알트코인 교체 매매 전략의 역사적 성과(수익률, MDD, 승률)를 검증 및 고도화합니다.
- **백테스트 시뮬레이션 알고리즘 (개선 모델)**:
  1. **과거 데이터 수집 & 로컬 캐싱**: 빗썸 원화 마켓 주요 알트코인들의 일봉 데이터(시가/고가/저가/종가/거래대금)를 수집하여 `data/` 폴더에 캐싱.
  2. **일별 공통 시장 필터 판정**: 매일 09:00 시점 업비트 BTC 220일 SMA ±2% 버퍼 히스테리시스 적용.
     - 하락장(Bear) 진입 시: 보유 중인 모든 알트코인을 즉시 전량 매도하여 100% 현금(KRW) 보존.
  3. **일별 개별 손절(Stop-Loss) 장치**:
     - 보유 종목이 매수가 대비 설정된 손절 비율(기본 `-8%`) 이하로 하락 시, 월요일이 아니더라도 당일 즉시 손절 매도하여 꼬리 리스크(폭락) 차단.
  4. **주간 리밸런싱 및 개별 추세 필터 (월요일 09:00)**:
     - 상승장(Bull) 유지/진입 시:
       - 직전 7일 일평균 거래대금 상위 N개 알트코인 추출 (BTC, ETH 제외)
       - **개별 추세 필터(Trend Filter)**: 현재가가 자신의 20일(또는 50일) SMA 이상인 상승 추세 종목만 1차 통과.
       - 통과된 종목 중 직전 모멘텀(14일/21일/30일) 수익률 상위 N개(최대 4개) 종목 최종 선정.
       - **동적 현금 관리**: 조건을 충족하는 종목 수만큼만 균등 매수하고, 충족 종목이 부족할 경우 잔여 비중은 100% 현금으로 안전하게 보존.
  5. **거래 비용 모델**: 매수/매도 시 빗썸 기본 수수료(0.04%~0.05%) 및 슬리피지(`--slippage`) 적용.
### 5.3. 알트코인 후보 전략 1: 래리 윌리엄스 변동성 돌파 전략 (`src/backtest_strategy1_vbo.py`)
- **개념**: 당일 시가 대비 전일 변동폭(고가 - 저가)의 K배 이상 상승 돌파 시 진입하고, 다음 날 09:00에 전량 청산하여 밤샘 리스크 없이 당일 폭발 추세만 향유하는 전략.
- **알고리즘 사양**:
  1. **시장 필터**: BTC 220일 SMA ±2% 버퍼 (상승장일 때만 신규 매수 허용)
  2. **목표 매수가 계산**: `목표가 = 당일 시가 + (전일 고가 - 전일 저가) * K` (K = 0.4~0.6 기본 0.5)
  3. **진입 조건**: 당일 장중 고가 >= 목표가 도달 시 목표가에 매수 체결
  4. **청산 조건**: 익일 09:00 시가(또는 종가)에 전량 매도 (24시간 단기 보유)
  5. **자산 배분**: 당일 돌파 신호가 발생한 상위 N개(기본 4개) 종목에 균등 분할 투자, 미진입 잔여 자금은 현금 보유.

### 5.4. 알트코인 후보 전략 2: 20일 채널 돌파 + ATR 트레일링 스탑 (`src/backtest_strategy2_turtle.py`)
- **개념**: 터틀 트레이딩의 채널 돌파(Donchian Channel) 기법으로 대세 상승 추세에 진입하고, 최고점 대비 ATR 변동폭 배수만큼 하락 시 익절/손절하는 추세 추종 전략.
- **알고리즘 사양**:
  1. **시장 필터**: BTC 220일 SMA ±2% 버퍼 (상승장일 때만 신규 매수 허용, 하락장 시 전량 현금화)
  2. **진입 조건**: 알트코인 현재 종가가 직전 20일간의 최고가를 상향 돌파 시 매수
  3. **청산 조건**: 진입 후 기록한 최고점(Peak Price) 대비 `2.0 * ATR(14)` 하락 시 트레일링 스탑 청산
  4. **자산 배분**: 동시 보유 최대 N개(기본 4개, 각 25%) 분할 매수.

### 5.5. 알트코인 후보 전략 3: 변동성 역가중 모멘텀 포트폴리오 (`src/backtest_strategy3_risk_parity.py`)
- **개념**: 레이 달리오의 리스크 패리티 모델을 적용하여, 선정된 알트코인의 변동성 역수($1/\sigma$)에 비례하여 투자 비중을 동적 가중 배분하는 위험 통제형 모멘텀 전략.
- **알고리즘 사양**:
  1. **시장 필터**: BTC 220일 SMA ±2% 버퍼 (상승장일 때만 신규 매수 허용, 하락장 시 전량 현금화)
  2. **종목 선정**: 월요일 09:00, 7일 거래대금 TOP 10 중 14일 수익률 TOP 4 종목 선정
  3. **변동성 역가중 배분**: 최근 20일 일일 수익률 표준편차($\sigma_i$)의 역수($w_i = (1/\sigma_i) / \sum(1/\sigma_k)$)로 투자 비중 결정
  4. **리밸런싱**: 매주 월요일 종목 및 비중 재조정.

### 5.6. 알트코인 후보 전략 4: 상승장 단기 과매도 반등 (`src/backtest_strategy4_mean_reversion.py`)
- **개념**: 비트코인 상승장 환경 속에서, 단기 패닉셀로 과매도(RSI < 35) 구간에 도달한 우량 알트코인을 저점 분할 매수한 후 평균 회귀(Mean Reversion) 시 익절하는 역추세 전략.
- **알고리즘 사양**:
  1. **시장 필터**: BTC 220일 SMA ±2% 버퍼 (상승장일 때만 신규 매수 허용, 하락장 시 전량 현금화)
  2. **진입 조건**: 거래대금 상위 알트코인 중 RSI(14) < 35 과매도 영역 진입 시 매수
  3. **청산 조건**: RSI(14) >= 55 반등 시 익절, 또는 진입 후 7일 경과 시 시간 청산, 또는 -8% 손절
  4. **자산 배분**: 동시 보유 최대 N개(기본 4개, 각 25%) 분할 매수.

### 5.7. 빗썸 서브 전략: 이더리움 SuperTrend + 50일 SMA 추세 추종 백테스트 요구사항 (`src/backtest_bithumb_eth.py`)
- **개념**: 이더리움(ETH)의 대세 상승 추세를 끝까지 추종하여 고수익(CAGR 50%+)을 창출하고, 하락장 진입 시 100% 현금화하여 MDD(-25%)를 통제하는 이더리움 단독 고수익/저낙폭 퀀트 모델 검증.
- **알고리즘 사양**:
  1. **대추세 필터 (50일 SMA)**: 일봉 종가가 50일 SMA 이상일 때만 매수 포지션 진입/유지 허용 (50일 SMA 이탈 시 전량 매도/관망).
  2. **동적 변동성 추세 지표 (SuperTrend 7, 3.5)**:
     - `ATR(7)` 및 승수 `3.5` 적용.
     - SuperTrend 상승 밴드(Green) 유지 + 50일 SMA 상회 시 **이더리움 100% 집중 매수/보유**.
     - SuperTrend 하락 밴드(Red) 전환 또는 50일 SMA 하향 이탈 시 **즉시 전량 매도 후 100% 현금 보존**.
  3. **비용 모델**: 빗썸 기본 수수료 0.04% 및 슬리피지 0.05% 적용.
  4. **결과물 저장**: `backtest/sub_eth/backtest_bithumb_eth_report_YYYYMMDD_HHMMSS.md` 및 `backtest/sub_eth/backtest_bithumb_eth_result_YYYYMMDD_HHMMSS.png`

---

## 6. PWA 기반 웹 대시보드 및 실시간 전략 차트 요구사항
- **개념**: 별도의 유료 백엔드 서버 없이 GitHub Pages 무료 호스팅(`https://haksoo0918.github.io/quant-bitcoin/`) 및 브라우저 업비트 공개 API 연동을 통해, **오전 9시 확정 일봉 데이터를 스마트 로컬 캐싱**하여 지표(SMA, ATR, SuperTrend)를 직접 연산하고 **전략별 인터랙티브 시각화 차트**를 실시간으로 제공하는 독립형 PWA 대시보드입니다.
- **아키텍처 및 디렉토리 구조**:
  - `docs/` 디렉토리를 GitHub Pages 배포 루트 및 로컬 독립 실행 루트로 사용
  - `docs/index.html`: 반응형 SPA 대시보드 (오전 9시 지표 연산 엔진, 전략별 Canvas 차트 뷰, 다크 모드, PWA)
  - `docs/data/status.json`: PWA 대시보드 상태 및 60일 시계열 차트 스냅샷 데이터 (GitHub Pages CORS 차단 방어 및 고속 렌더링용)
  - `docs/manifest.json`: PWA 앱 설치 메타데이터 (앱 이름, 테마 색상, 시작 URL, 아이콘)
  - `docs/service-worker.js`: 오프라인 캐싱 및 Chart.js / PWA 지원 서비스 워커
  - `docs/icons/`: PWA 앱 아이콘 (192x192, 512x512)
- **GitHub Pages CORS 방어 및 스마트 로컬 캐싱 파이프라인**:
  - **CORS 보안 방어**: 브라우저가 업비트 외부 API를 직접 호출할 때 발생하는 CORS 차단(`net::ERR_FAILED`)을 방지하기 위해, 동일 출처(Origin)의 `docs/data/status.json`에 최근 60일 시계열 차트 데이터를 포함하여 배포.
  - **원리**: 매일 오전 09:00 KST에 확정된 일봉 지표(220 SMA, 50 SMA, 14 ATR, SuperTrend 7/3.5)는 당일 하루 동안 불변하는 고정 상수이므로, `status.json` 스냅샷을 1순위로 즉시 렌더링하고 브라우저 `localStorage`에 스마트 캐싱.
  - **트래픽 및 계산량 최소화**: 당일 재접속 시 과거 일봉 캔들 재다운로드 및 지표 연산을 완전히 스킵(트래픽 0 KB, 계산 0ms)하고 0.01초 만에 화면 및 차트를 즉시 렌더링.
  - **실제 봇 매매 상태와 100% 일치**: 장중 휩소(노이즈)로 인한 화면 신호 흔들림을 방지하고, 아침 09:05 봇이 실제로 집행한 포지션과 하루 종일 100% 일치하는 일관된 대시보드 제공.
  - **자동 만료 및 갱신**: 익일 09:00 KST 경과 시 캐시가 자동으로 만료되어 새 일봉 데이터를 1회 자동 수신 및 갱신.
- **전략별 인터랙티브 Canvas 차트 뷰 (Chart.js)**:
  - **공통 사양**: `Chart.js` 초경량 Canvas 엔진 적용, PWA 오프라인 캐싱 지원, 모바일 터치 툴팁, `hs-style` 테마(다크/라이트) 자동 동기화.
  - **1. 업비트 BTC 메인 차트**: 최근 60~90일 종가 추세선 + 220일 SMA 중심선 + 상하한 ±2% 버퍼 채널 음영 밴드.
  - **2. 업비트 ETH 메인 차트**: 최근 60~90일 종가 추세선 + 50일 SMA 기준선 + 상하한 ±1.5 ATR 변동성 돌파 채널 밴드.
  - **3. 이더리움 서브 전략 차트 (SuperTrend)**: 최근 60~90일 종가선 + 50일 SMA선 + SuperTrend 상승(초록 🟢)/하락(빨강 🔴) 동적 추세 밴드.
  - **차트 탭 인터페이스**: 모바일 화면에서 한 손가락으로 가볍게 전환할 수 있는 `[BTC 220일 버퍼] | [ETH 50일 채널] | [이더리움 SuperTrend]` 상단 탭 제공.
- **주요 UI/UX 기능 및 디자인 시스템 (hs-style 적용)**:
  - **디자인 시스템 (hs-style)**:
    - **핵심 철학**: 도구의 명확성과 정밀성을 위한 모던 딥 틸 & 에메랄드 포인트 기반 클린/미니멀 디자인 시스템.
    - **컬러 팔레트**:
      - Light: Primary Deep Teal (`#0d9488`), Background (`#ffffff`), Surface (`#f8f9fb`), Border (`#e5e7eb`), Text (`#1a1d2e`), Muted (`#6b7280`).
      - Dark: Primary Emerald Teal (`#2dd4bf`), Background (`#0a0e27`), Surface (`#1a1f3a`), Border (`#2d3548`), Text (`#f0f4f8`), Muted (`#8b92a9`).
      - Semantic: Success (`#10b981` / `#6ee7b7`), Warning (`#f59e0b` / `#fbbf24`), Danger (`#ef4444` / `#f87171`).
    - **타이포그래피 & 아이콘**:
      - 본문 및 레이블: `Noto Sans KR` (가독성 최우선, `word-break: keep-all;`).
      - 금융 수치 및 코드/지표: `D2Coding` 고정폭 폰트 적용으로 숫자 자릿수 및 시인성 극대화.
      - 아이콘: `Lucide Icons` 표준 일관 선 두께(`1.75px`) 적용.
    - **모바일 퍼스트**: `<= 640px` 모바일 뷰포트 최우선 레이아웃, 터치 타겟 44px 이상 확보, 버튼 텍스트 줄바꿈 방지.
  - **종합 행동 가이드 배너**: 당일 아침 확정 기준 메인/서브 전략 즉시 매매 행동 요약
  - **메인 전략 카드 (50:50 듀얼 모멘텀)**: BTC (220일 SMA ±2%), ETH (50일 SMA ± 1.5 ATR) 현재가, 기준 지표, 전략 판정 뱃지 표시
  - **서브 전략 카드 (이더리움 SuperTrend + 50일 SMA)**: 이더리움 단독 SuperTrend(7, 3.5) 추세선 및 50일 SMA 기준 판정 뱃지, 당일 포지션(100% 매수 / 100% 현금화) 표시
  - **장중 실시간 시세 버튼**: 언제든 현재가를 재동기화하여 이탈 여부 및 안전 거리 확인
  - **PWA 앱 설치**: 모바일/데스크톱 홈 화면에 바로가기 앱으로 설치 지원



